module ExtendedIntervals

import Base: convert, promote_type, show, showcompact,
    read, write, string, hash, sizeof,
    issubnormal, isfinite, isreal, isinf, isnan,
    (==),(!=),(<),(<=),(>=),(>), isless, isequal,
    (+),(-),(*),(/)


end # module
